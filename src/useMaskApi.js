import { useState, useEffect, useCallback } from "react";

const useMaskApi = () => {
  const [maskData, setMaskData] = useState([]);
  const fetchMaskData = useCallback();
};

export default useMaskApi;
